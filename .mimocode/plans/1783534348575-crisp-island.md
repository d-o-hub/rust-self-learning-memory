# Plan: Improve Installation Documentation (Issue #770)

## Problem
Users cannot easily install `do-memory-cli` — pre-built binaries exist on GitHub Releases but are undocumented. The README only shows "build from source" which requires system deps (`libssl-dev`, `pkg-config`).

## Changes

### 1. Root `README.md` — Add "Install Pre-built Binary" section

Insert a new **"Install Pre-built Binary"** section between the existing "Build Dependencies" (line 188) and "Installation" (line 199) sections. This becomes the **recommended** install path.

Content:
- Point to [GitHub Releases](https://github.com/d-o-hub/rust-self-learning-memory/releases/latest)
- Table of available binaries (Linux x64, Linux ARM64, macOS Intel, macOS Apple Silicon, Windows x64)
- One-liner example for Linux x64: download, extract, move to PATH
- Mention `cargo-binstall` as an alternative: `cargo binstall do-memory-cli`

### 2. Root `README.md` — Restructure existing "Installation" section

Rename existing "Installation" section to **"Build from Source"** and add a note that this is for contributors/developers. Keep existing content intact.

### 3. `memory-cli/README.md` — Add pre-built binary option

The existing "Installation" > "From Source" section (line 22-32) stays. Add a **"Pre-built Binary (Recommended)"** subsection above "From Source" with:
- Download link to GitHub Releases
- One-liner for Linux x64
- `cargo-binstall` alternative
- Keep existing "From Source" and "Feature Flags" subsections as-is

### Files to modify
- `/home/do/git/d-o-hub/rust-self-learning-memory/README.md` (lines ~188-216)
- `/home/do/git/d-o-hub/rust-self-learning-memory/memory-cli/README.md` (top of file)

### Out of scope
- Publishing to crates.io (separate effort, not requested by issue)
- npm distribution (not applicable — pure Rust project)
- Adding cargo-dist installers (e.g. shell script installer) — separate feature

## Verification
- Read both modified files to confirm formatting and links
- Verify GitHub Releases URL is correct: `https://github.com/d-o-hub/rust-self-learning-memory/releases/latest`
- Ensure no broken markdown rendering (headers, tables, code blocks)
