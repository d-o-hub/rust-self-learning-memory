# GOAP Plan: Resolve Open GitHub Issues

## Overview

3 open issues to resolve, in priority order:

| Issue | Title | Effort | Dependencies |
|-------|-------|--------|-------------|
| #800 | Workspace lint hygiene | **CLOSE (already done)** | None |
| #805 | Release drift: v0.1.34 | **Medium** | #800 closed |
| #770 | CLI not on crates.io | **Investigate** | Separate from release |

## Issue #800 — Lint Hygiene: CLOSE

**Status**: Already fully implemented by PR #803 and existing config.

Evidence:
- Root `Cargo.toml` has `unwrap_used = "warn"`, `expect_used = "warn"`, `panic = "warn"`
- All sub-crates have `[lints] workspace = true`
- PR #803 added `#![allow(...)]` to 25 test/bench/example files
- `cargo clippy --all-targets -- -D warnings` passes clean

**Action**: Close issue #800 with comment explaining it's resolved.

## Issue #805 — Release v0.1.34

### Files to Modify

| File | Change |
|------|--------|
| `Cargo.toml` (root) | `version = "0.1.33"` → `"0.1.34"` |
| `memory-core/Cargo.toml` | `version = "0.1.33"` → `"0.1.34"` |
| `memory-storage-turso/Cargo.toml` | version + dep versions |
| `memory-storage-redb/Cargo.toml` | version + dep versions |
| `memory-mcp/Cargo.toml` | version + dep versions |
| `memory-cli/Cargo.toml` | version + dep versions |
| `memory-test-utils/Cargo.toml` | version (if exists) |
| `benches/Cargo.toml` | version (if exists) |
| `CHANGELOG.md` | Add `## [0.1.34]` section |
| `plans/ROADMAPS/ROADMAP_ACTIVE.md` | Update Released Version to v0.1.34 |
| `plans/STATUS/CURRENT.md` | Update Released Version, reset commit count |
| `VERSION` file | Update to 0.1.34 (if exists) |

### Execution Steps

1. **Create branch**: `git checkout -b release/v0.1.34`
2. **Bump versions**: Use `sed` or manual edits across all Cargo.toml files
3. **Update CHANGELOG**: Add v0.1.34 section with commit summaries
4. **Update docs**: ROADMAP_ACTIVE.md, STATUS/CURRENT.md
5. **Commit**: `git commit -m "chore(release): bump to v0.1.34"`
6. **Push branch**: `git push origin release/v0.1.34`
7. **Create PR**: `gh pr create` targeting main
8. **Wait for CI**: All checks must pass
9. **Merge PR**: `gh pr merge --merge`
10. **Tag release**: `git tag v0.1.34 && git push origin v0.1.34`
11. **Verify**: `release.yml` workflow triggers automatically

### Quality Gates
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo fmt --check` passes
- [ ] `cargo nextest run --all` passes
- [ ] All Cargo.toml versions consistent
- [ ] CHANGELOG has entry for v0.1.34
- [ ] PR merged to main
- [ ] Tag v0.1.34 pushed
- [ ] release.yml workflow completes successfully

## Issue #770 — CLI Publishing: DEFER

**Finding**: `do-memory-cli` has no `publish = false` but is also absent from the publish workflow. Path dependencies would fail on crates.io.

**Options**:
1. Add to publish workflow with versioned deps (complex, requires dep version management)
2. Add `publish = false` to memory-cli/Cargo.toml (simple, documents reality)
3. Document build-from-source instructions (issue already asks for this)

**Recommendation**: Defer to a separate PR. The release (v0.1.34) should not be blocked by this.

## Execution Order

```
1. Close #800 (comment)
2. Create release branch
3. Bump all versions to 0.1.34
4. Update CHANGELOG, ROADMAP, STATUS
5. Commit + push
6. Create PR
7. Wait for CI (monitor like PR #803)
8. Merge PR
9. Tag v0.1.34
10. Verify release workflow
```

## Verification

1. `gh issue view 800 --json state` → CLOSED
2. `grep 'version' Cargo.toml` → 0.1.34
3. `git tag -l v0.1.34` → exists
4. `gh run list --workflow=release.yml --limit 1` → success
5. `gh release view v0.1.34` → published
