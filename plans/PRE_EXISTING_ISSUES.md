# Pre-Existing Issues

Documented: 2026-05-16 | Generated by `./scripts/quality-gates.sh` run

## Blocking Issues

None currently — `memory-core/src/context/dag/assembler.rs` was at 505 LOC (limit: 500) and
was fixed by condensing documentation comments (commit `e31c943b`).

## Non-Blocking Warnings

### 1. GOAP Missing Plan Files
**Severity**: Non-blocking (informational)
**Source**: `scripts/quality-gates.sh` — GOAP checks
**Details**: Several expected GOAP plan files are absent from `plans/`:
- `GOAP_AGENT_IMPROVEMENT_PLAN.md`
- `GOAP_PERFORMANCE_PLAN.md`
- `GOAP_SECURITY_PLAN.md`
These may be intentionally absent (consolidated into `GOAP_STATE.md` and `plans/ROADMAPS/ROADMAP_ACTIVE.md`).

### 2. Root `llms.txt` Location
**Severity**: Non-blocking (informational)
**Source**: `scripts/quality-gates.sh` — Root documentation check
**Details**: `llms.txt` exists in the project root. Per project conventions, it should be in
`plans/`. However, `llms.txt` is conventionally placed at the repo root for AI agent discovery
(llms.txt standard), so this may be intentional.

### 3. Docs Integrity — Broken Links
**Severity**: Non-blocking (informational)
**Source**: `scripts/quality-gates.sh` — Docs integrity check
**Details**: The `check-docs-integrity.sh` script reports broken internal links across
documentation and plan files. This is a known accumulation from multiple PRs and should
be addressed in a dedicated docs cleanup sprint.

### 4. Test File Sizes (>500 LOC)
**Severity**: Non-blocking (tests exempt from LOC gate)
**Source**: `scripts/quality-gates.sh` — Test file size gate
**Details**: 54 test files exceed the 500 LOC threshold. This is expected for comprehensive
integration and soak test suites. The source file gate only applies to `src/`, not `tests/`.

### 5. Doc Test — Missing `event_emitter_mode` Field (FIXED)
**Severity**: Blocking (compile error in doc tests)
**Source**: `cargo test --doc memory-core`
**Details**: The `MemoryConfig` doc test in `memory-core/src/types/config.rs` was missing
the `event_emitter_mode` field added in PR #544 (CloudEvents). Fixed in PR #548.
