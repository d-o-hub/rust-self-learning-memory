# Memory CLI Configuration - Unified Setup
# This is the canonical configuration for all memory-cli operations

[database]
# Primary SQLite database for episodes and patterns (source of truth)
turso_url = "file:./data/memory.db"
turso_token = ""

# Unified redb cache for all operations
redb_path = "./data/cache.redb"

[storage]
# Maximum episodes to cache in memory
max_episodes_cache = 1000

# Cache TTL in seconds
cache_ttl_seconds = 3600

# Connection pool size for database connections
pool_size = 10

[cli]
# Default output format (human, json, yaml)
default_format = "human"

# Enable progress bars for long operations
progress_bars = true

# Batch size for bulk operations
batch_size = 100

[monitoring]
# Enable metrics collection
enabled = true

# Health check intervals
health_check_interval_seconds = 30

[backup]
# Default backup directory
backup_dir = "./backups"

# Maximum backup age in days
max_backup_age_days = 30

# Compression enabled for backups
compress_backups = true

[logging]
# Log level (error, warn, info, debug, trace)
level = "info"

# Maximum log file size in MB
max_log_size_mb = 10

# Maximum number of log files to keep
max_log_files = 5